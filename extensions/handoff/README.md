# pi-handoff

Private extension that generates an editable context-transfer prompt and opens it in a new session.

Adapted from Pi's upstream [handoff example](https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/examples/extensions/handoff.ts).
